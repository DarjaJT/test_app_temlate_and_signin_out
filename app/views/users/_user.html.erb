<li>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %> <!-- Access is only open to the administrator -->
      | <%= link_to "delete", user, method: :delete, data: { confirm: "You sure?" } %>
  <% end %>
</li>